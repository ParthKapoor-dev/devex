import MoltenLogin from "@/components/mocks/molten/login";

// MOCK — direction A · Molten login. Does not redirect signed-in users.

export default function MoltenLoginPage() {
  return <MoltenLogin />;
}
